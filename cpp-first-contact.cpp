#include <bits/stdc++.h> //include every single function of c++

using namespace std; //with this, i can use the classes and functions of the std library directly

int main() {
    string s;

    getline(cin, s); //to read a whole line from the input

    cout << s << "\n";
}